@model IEnumerable<Benefit.Domain.Models.ApplicationUser>
@if (!Model.Any())
{
    <div>Пошук не дав жодних результатів</div>
}
else
{
    <div class="table-responsive">
        <table class="table table-striped table-bordered table-hover" id="sample-table-1">
            <thead>
                <tr>
                    <th>
                        Ім'я
                    </th>
                    <th>
                        Email
                    </th>
                    <th>
                        Телефон
                    </th>
                    <th>
                        Номер Карти
                    </th>
                    <th>
                        Дії
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var user in Model)
                {
                    <tr>
                        <td>
                            @user.FullName
                        </td>
                        <td>
                            @user.Email
                        </td>
                        <td>
                            @user.PhoneNumber
                        </td>
                        <td>
                            @user.CardNumber
                        </td>
                        <td>
                            <div class="visible-md visible-lg hidden-sm hidden-xs btn-group">
                                <a class="btn btn-xs btn-success" href="@Url.Action("Edit", new { id = user.Id })">
                                    <i class="icon-ok bigger-120"></i>
                                </a>

                                <a class="btn btn-xs btn-info" href="@Url.Action("Edit", new { id = user.Id })">
                                    <i class="icon-edit bigger-120"></i>
                                </a>

                                <a class="btn btn-xs btn-danger" href="@Url.Action("Delete", new { id = user.Id })">
                                    <i class="icon-trash bigger-120"></i>
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}
