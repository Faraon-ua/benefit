@model IEnumerable<Benefit.Domain.Models.Product>
@if (!Model.Any())
{
    <div>Пошук не дав жодних результатів</div>
}
else
{
    <div class="table-responsive">
        <table class="table table-striped table-bordered table-hover" id="sample-table-1">
            <thead>
                <tr>
                    <th>
                        Код товару
                    </th>
                     <th>
                        Назва
                    </th>
                    <th>
                        Категорія
                    </th>
                    <th>
                        Назва постачальника
                    </th>
                    <th>
                        Наявність
                    </th>
                    <th>
                        Дії
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var product in Model)
                {
                    <tr id="tr-@product.Id">
                        <td>
                            @product.SKU
                        </td>
                         <td>
                            @product.Name
                        </td>
                        <td>
                            @product.Category.Name
                        </td>
                        <td>
                            @product.Seller.Name
                        </td>
                        <td>
                            @product.Amount
                        </td> 
                        <td>
                            <div class="visible-md visible-lg hidden-sm hidden-xs btn-group">
                                <a class="btn btn-xs btn-info" href="@Url.Action("CreateOrUpdate", new { id = product.Id })" target="_blank">
                                    <i class="icon-edit bigger-120"></i>
                                </a>

                                <a class="btn btn-xs btn-warning" onclick="LockUnlock('@product.Id');">
                                    <i class="lockUser-@product.Id icon-lock bigger-120"></i>
                                </a>

                                <a class="btn btn-xs btn-danger" onclick="DeleteProduct('@product.Id', '@product.Name');">
                                    <i class="icon-trash bigger-120"></i>
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}
